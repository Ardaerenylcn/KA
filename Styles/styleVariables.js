import { StyleSheet } from 'react-native';

const styleVariables = StyleSheet.create({
	shadow: {
		shadowOffset: {
			width: 0,
			height: 2,
		},
		shadowOpacity: 0.3,
		shadowRadius: 4,
		elevation: 5,
	},
});

export default styleVariables;
